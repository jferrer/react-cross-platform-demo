import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const Projects = () => (
  <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <Text h4>Projects Screen</Text>
  </View>
);

export default Projects;
