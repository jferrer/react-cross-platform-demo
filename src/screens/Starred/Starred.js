import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const Starred = () => (
  <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <Text h4>Starred Screen</Text>
  </View>
);

export default Starred;
