import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const Week = () => (
  <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <Text h4>Week Screen</Text>
  </View>
);

export default Week;
