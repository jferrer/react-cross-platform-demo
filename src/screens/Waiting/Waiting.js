import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const Waiting = () => (
  <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <Text h4>Waiting Screen</Text>
  </View>
);

export default Waiting;
